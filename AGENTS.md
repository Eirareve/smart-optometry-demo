# AGENTS.md

## 项目依据

- 开始工作前必须完整阅读根目录 `PROJECT_PLAN.md`；它是本项目的最高规划依据。
- 当前产品阶段是网页 Demo。按阶段完成可验证的小目标，不提前扩展产品范围。
- 当本文件与 `PROJECT_PLAN.md` 冲突时，以 `PROJECT_PLAN.md` 为准。
- 未来正式厂家资料若与现有规划冲突，应先更新文档，再修改实现。

## 当前范围

- 当前只验证智能验光设备型应用的页面体验、检测流程、数据抽象和报告交互。
- V0.1 使用 Mock Device；真实设备接入必须等待厂家正式资料。
- 已知展示字段仅包括 SPH、CYL、AXIS。
- 未知 17 项数据只允许使用 `metric_01`～`metric_17`，页面名称为“扩展检测指标 01”～“扩展检测指标 17”。
- 所有模拟设备状态和验光结果必须醒目标记为“模拟数据”或 “DEMO”。
- 模拟结果不得描述为医疗诊断或处方依据。

## 架构与编码约束

- UI、验光流程服务和设备适配层必须解耦；UI 不得依赖具体设备实现。
- 设备通讯统一置于 `DeviceAdapter` 抽象之后；V0.1 计划使用 `MockDeviceAdapter`。
- 不把所有业务逻辑写入 React 页面组件。
- 验光流程使用明确状态机，不能只用单个 loading 状态表达。
- 扩展指标保持可扩展结构，并保留原始数据 `rawData`。
- 不为了扩大功能而引入商城、配镜、3D 试戴、支付、库存、订单、复杂权限或医疗系统集成。

## 禁止事项

- 不得虚构厂家 API、SDK、DLL、通讯协议、设备能力、错误码或返回示例。
- 不得为未知 17 项数据编造医学名称、单位、范围或诊断含义。
- 不得提交真实患者数据、个人敏感信息、密钥、`.env` 或受保密约束的厂家 SDK/DLL。
- 未获得正式资料前，不声称真实设备已经接入。
- 未经当前任务明确要求，不跨阶段实现功能。

## 测试与验证

- 安装依赖：`npm install`
- 启动开发环境：`npm run dev`
- 类型检查：`npm run typecheck`
- 生产构建：`npm run build`
- 代码检查：`npm run lint`
- 单次运行测试：`npm test`
- 测试监听模式：`npm run test:watch`
- 修改代码后必须运行项目已有的 lint、typecheck、test 和 build，并如实报告结果；不得静默吞掉错误。
- 文档类改动应检查文件结构、链接、术语和项目范围是否一致。

## 文档维护

- 每完成一个主要阶段，同步维护相关 Markdown 文档。
- 重要架构选择记录到 `docs/07-decisions.md`。
- 设备资料、字段映射与接入进展更新到 `docs/03-device-integration.md`。
- 未确认的厂家信息统一写 `TBD`，不得猜测。
